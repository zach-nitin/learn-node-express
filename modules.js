//Modules - to split your code
//CommonJs, every file is module (by default)
//Modules - Encapsulated Code (only share minimum)

const names = require("./modules1");
const sayHi = require("./modules2");
const data = require("./modulesAlternateWay");
require("./mindgreande");

// console.log(data);

// sayHi(`susan`);
// sayHi(names.john);
// sayHi(names.peter);
